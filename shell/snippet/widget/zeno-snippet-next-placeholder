#autoload

emulate -L zsh

local -a out

out=( "${(f)$(zeno-call-client-and-fallback 'next-placeholder' "${BUFFER}")}" )

if [[ $out[1] == success && -n $out[2] ]]; then
  BUFFER=$out[2]
  CURSOR=$out[3]
fi
