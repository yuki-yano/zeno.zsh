#autoload

mode=$1
input=$2

if [[ -n $ZENO_ENABLE_SOCK ]]; then
  if [[ -S $ZENO_SOCK ]]; then
    zeno-client "--zeno-mode=${mode} ${input}"
    return
  fi
  zeno-start-server
fi
echo "${input}" | zeno --zeno-mode=${mode}
